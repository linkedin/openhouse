plugins {
    id 'openhouse.java-minimal-conventions'
    id 'openhouse.client-codegen-convention'
    id 'openhouse.maven-publish'
}

ext {
    codeGenForService = ":services:jobs"
}

apply from: "${project(':client:common').file("codegen.build.gradle")}"
